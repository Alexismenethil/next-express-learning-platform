# Used in browser-side requests and client components
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Used by server components when the frontend runs in Docker
INTERNAL_API_URL=http://localhost:4000/api

# Human-friendly app name surfaced in the UI
NEXT_PUBLIC_APP_NAME=Product Catalog Learning Platform
